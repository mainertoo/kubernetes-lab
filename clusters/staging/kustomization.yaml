apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - flux-system
  - infrastructure.yaml
  - apps.yaml
  - infra-repositories.yaml

  # ✅ production overlays (same cluster)
  - infra-secrets-prod.yaml
  - apps-production.yaml

  # ✅ volsync controller install (Flux Kustomization object)
  - ../production/volsync.yaml