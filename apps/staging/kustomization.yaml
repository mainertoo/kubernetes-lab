apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Staging cluster app registry. Empty by default — add one line per app
# to opt it into the staging cluster. Pattern matches apps/production/.
resources:
  # meshcentral promoted to production 2026-06-14 (apps/production/meshcentral)
  - chaptarr/kustomization.yaml
  # EMR evaluation pair (2026-09-03) — throwaway, no backups.
  - openemr/kustomization.yaml
  - openmrs/kustomization.yaml
