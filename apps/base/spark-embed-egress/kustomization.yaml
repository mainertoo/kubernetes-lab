apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# No top-level `namespace:` — the namespace itself is one of the resources,
# and each resource carries its own metadata.namespace.
resources:
  - spark-embed-egress-namespace.yaml
  - spark-embed-service.yaml
  - spark-llm-service.yaml
