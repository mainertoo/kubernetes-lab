apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: memos
  namespace: memos
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-rbd