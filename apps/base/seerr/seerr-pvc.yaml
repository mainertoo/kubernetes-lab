apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seerr
  namespace: seerr
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: cephfs