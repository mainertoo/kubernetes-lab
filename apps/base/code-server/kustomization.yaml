apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: code-server
resources:
  # PVC owned by volsync-v2 (bootstrap on first deploy, plain after settle).
  # See apps/production/code-server/kustomization.yaml for the volsync wiring.
  # - code-server-pvc.yaml
  - code-server-release.yaml
  - code-server-ingressroute.yaml
  # - code-server-servers-transport.yaml
  - code-server-namespace.yaml
  - code-server-secret.sops.yaml