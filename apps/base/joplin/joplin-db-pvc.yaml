apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: joplin-db
  namespace: joplin
spec:
  storageClassName: ceph-rbd
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 10Gi