apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: airtrail-uploads
  namespace: airtrail
  labels:
    backup: daily
    backup-engine: kopia
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd
  resources:
    requests:
      storage: 2Gi
