apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - zilean-data-pvc.yaml
  - zilean-tmp-pvc.yaml
  - zilean-pgadmin-data-pvc.yaml
  - zilean-release.yaml
  - zilean-env.sops.yaml
  - zilean-ingressroute.yaml
  # pgAdmin still in place but its target (zilean-postgres Service) is gone
  # after Phase 5c. Either delete pgadmin entirely or update
  # zilean-pgadmin-config.sops.yaml to point at zilean-cnpg-db-rw.
  - zilean-pgadmin.yaml
  - zilean-pgadmin-ingressroute.yaml
  - zilean-pgadmin-env.sops.yaml
  - zilean-pgadmin-config.sops.yaml