apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: notifiarr-shared
  namespace: media
spec:
  storageClassName: cephfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi