apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: notifiarr
  namespace: media
spec:
  storageClassName: ceph-rbd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi