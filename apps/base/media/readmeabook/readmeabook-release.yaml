apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: readmeabook
  namespace: media
spec:
  interval: 30m
  releaseName: readmeabook

  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system

  maxHistory: 2

  install:
    createNamespace: false
    remediation:
      retries: 3

  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3

  uninstall:
    keepHistory: false

  values:
    controllers:
      main:
        strategy: Recreate

        initContainers:
          init-app-storage:
            image:
              repository: busybox
              tag: stable
            command: ["/bin/sh", "-c"]
            args:
              - |
                set -e
                echo "Initializing ReadMeABook storage..."
                mkdir -p /storage/config
                mkdir -p /storage/cache
                mkdir -p /storage/pgdata
                mkdir -p /storage/redis
                chown -R 1000:1000 /storage || true
                chmod -R 0775 /storage || true
                echo "Storage contents:"
                ls -la /storage || true
                echo "Done."
            securityContext:
              runAsUser: 0
              runAsGroup: 0
              runAsNonRoot: false
              allowPrivilegeEscalation: false
              capabilities:
                drop: ["ALL"]
                add: ["CHOWN", "DAC_OVERRIDE", "FOWNER"]

        containers:
          main:
            image:
              repository: ghcr.io/kikootwo/readmeabook
              tag: latest
              pullPolicy: IfNotPresent
            env:
              TZ: America/Los_Angeles
              PUID: "1000"
              PGID: "1000"
              PUBLIC_URL: "https://readmeabook.lab.mainertoo.com"
            securityContext:
              runAsUser: 0
              runAsGroup: 0
              runAsNonRoot: false

            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: 3030
                  initialDelaySeconds: 45
                  periodSeconds: 10
                  timeoutSeconds: 5
                  failureThreshold: 6
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: 3030
                  initialDelaySeconds: 20
                  periodSeconds: 10
                  timeoutSeconds: 5
                  failureThreshold: 6
              startup:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: 3030
                  initialDelaySeconds: 10
                  periodSeconds: 10
                  timeoutSeconds: 5
                  failureThreshold: 30

    defaultPodOptions:
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch

    service:
      main:
        controller: main
        type: ClusterIP
        ports:
          http:
            port: 3030

    persistence:
      appdata:
        enabled: true
        existingClaim: readmeabook
        advancedMounts:
          main:
            init-app-storage:
              - path: /storage
            main:
              - path: /app/config
                subPath: config
              - path: /app/cache
                subPath: cache
              - path: /var/lib/postgresql/data
                subPath: pgdata
              - path: /var/lib/redis
                subPath: redis

      qnap-data:
        enabled: true
        existingClaim: qnap-media-pvc
        advancedMounts:
          main:
            main:
              - path: /data

    resources:
      requests:
        cpu: 50m
        memory: 256Mi
      limits:
        memory: 1Gi