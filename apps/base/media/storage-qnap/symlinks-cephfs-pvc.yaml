apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: symlinks-cephfs
  namespace: media
spec:
  storageClassName: cephfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi