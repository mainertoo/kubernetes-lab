apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lidarr
  namespace: media
spec:
  storageClassName: ceph-rbd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi