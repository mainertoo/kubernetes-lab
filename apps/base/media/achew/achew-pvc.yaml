apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: achew-config
  namespace: media
  labels:
    backup: daily
    backup-engine: kopia
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd
  resources:
    requests:
      storage: 1Gi
