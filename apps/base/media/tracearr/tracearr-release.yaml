apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tracearr
  namespace: media
spec:
  interval: 30m
  releaseName: tracearr

  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system

  maxHistory: 2

  install:
    createNamespace: false
    remediation:
      retries: 3
    timeout: 15m

  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
    timeout: 15m

  uninstall:
    keepHistory: false

  values:
    controllers:
      tracearr:
        initContainers:
          wait-for-deps:
            image:
              repository: busybox
              tag: "1.36"
              pullPolicy: IfNotPresent
            command:
              - /bin/sh
              - -c
            args:
              - |
                set -eux

                PG_HOST="tracearr-tracearr-timescale.media.svc.cluster.local"
                REDIS_HOST="tracearr-tracearr-redis.media.svc.cluster.local"

                until nslookup "${PG_HOST}" >/dev/null 2>&1; do
                  echo "waiting for DNS: ${PG_HOST}"
                  sleep 2
                done

                until nslookup "${REDIS_HOST}" >/dev/null 2>&1; do
                  echo "waiting for DNS: ${REDIS_HOST}"
                  sleep 2
                done

                until nc -z -w 2 "${PG_HOST}" 5432; do
                  echo "waiting for postgres: ${PG_HOST}:5432"
                  sleep 2
                done

                until nc -z -w 2 "${REDIS_HOST}" 6379; do
                  echo "waiting for redis: ${REDIS_HOST}:6379"
                  sleep 2
                done

                echo "deps are reachable"

        containers:
          app:
            image:
              repository: ghcr.io/connorgallopo/tracearr
              tag: latest
              pullPolicy: IfNotPresent

            env:
              TZ: America/Los_Angeles
              PORT: "3000"
              NODE_ENV: production
              LOG_LEVEL: info

              # Use FQDN so the app never depends on short-name search behavior
              REDIS_URL: redis://tracearr-tracearr-redis.media.svc.cluster.local:6379
              CORS_ORIGIN: "*"

            envFrom:
              - secretRef:
                  name: tracearr-secret

            ports:
              - name: http
                containerPort: 3000
                protocol: TCP

      redis:
        containers:
          main:
            image:
              repository: redis
              tag: 8-alpine
              pullPolicy: IfNotPresent
            command:
              - redis-server
              - --appendonly
              - "yes"

      timescale:
        initContainers:
          init-timescale-pgdata:
            image:
              repository: busybox
              tag: "1.36"
              pullPolicy: IfNotPresent
            command:
              - /bin/sh
              - -c
            args:
              - |
                set -eux
                mkdir -p /var/lib/postgresql/data/pgdata
                chown -R 999:999 /var/lib/postgresql/data/pgdata
            securityContext:
              runAsUser: 0
              runAsGroup: 0
              runAsNonRoot: false

        containers:
          main:
            image:
              repository: timescale/timescaledb
              tag: latest-pg16
              pullPolicy: IfNotPresent
            env:
              POSTGRES_DB: tracearr
              POSTGRES_USER: tracearr
              PGDATA: /var/lib/postgresql/data/pgdata
            envFrom:
              - secretRef:
                  name: tracearr-secret

    service:
      tracearr:
        controller: tracearr
        type: ClusterIP
        ports:
          http:
            port: 80
            targetPort: 3000

      tracearr-redis:
        controller: redis
        type: ClusterIP
        ports:
          redis:
            port: 6379
            targetPort: 6379

      tracearr-timescale:
        controller: timescale
        type: ClusterIP
        ports:
          postgres:
            port: 5432
            targetPort: 5432

    persistence:
      redis-data:
        enabled: true
        existingClaim: tracearr-redis
        advancedMounts:
          redis:
            main:
              - path: /data

      timescale-data:
        enabled: true
        existingClaim: tracearr-timescale
        advancedMounts:
          timescale:
            init-timescale-pgdata:
              - path: /var/lib/postgresql/data
            main:
              - path: /var/lib/postgresql/data

      timescale-shm:
        enabled: true
        type: emptyDir
        medium: Memory
        sizeLimit: 512Mi
        advancedMounts:
          timescale:
            main:
              - path: /dev/shm

    defaultPodOptions:
      automountServiceAccountToken: false