apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dispatcharr
  namespace: media
spec:
  interval: 30m
  releaseName: dispatcharr
  chart:
    spec:
      chart: app-template
      version: 3.7.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system

  install:
    createNamespace: false
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false

  values:
    controllers:
      main:
        initContainers:
          init-perms:
            image:
              repository: busybox
              tag: stable
            command:
              - /bin/sh
              - -lc
            args:
              - |
                set -e
                echo "Fixing perms..."
                mkdir -p /data /etc/profile.d
                chown -R 1000:1000 /data /etc/profile.d
                chmod -R 0775 /data /etc/profile.d
                ls -ld /data /etc/profile.d || true
            securityContext:
              runAsUser: 0
              runAsGroup: 0
              runAsNonRoot: false

        containers:
          main:
            image:
              repository: ghcr.io/dispatcharr/dispatcharr
              tag: 0.14.0
              pullPolicy: IfNotPresent
            env:
              - name: TZ
                value: America/Los_Angeles
              - name: DISPATCHARR_ENV
                value: aio
              - name: DISPATCHARR_LOG_LEVEL
                value: info
            ports:
              - name: http
                containerPort: 9191
                protocol: TCP

    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: "OnRootMismatch"

    service:
      main:
        controller: main
        type: ClusterIP
        ports:
          http:
            port: 9191

    persistence:
      data:
        enabled: true
        existingClaim: dispatcharr
        advancedMounts:
          main:
            main:
              - path: /data
            init-perms:
              - path: /data

      # Make /etc/profile.d writable for the entrypoint (fixes dispatcharr.sh permission denied)
      profile:
        type: emptyDir
        advancedMounts:
          main:
            main:
              - path: /etc/profile.d
            init-perms:
              - path: /etc/profile.d