apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dispatcharr
  namespace: media
spec:
  interval: 30m
  releaseName: dispatcharr
  chart:
    spec:
      chart: app-template
      version: 3.7.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system

  values:
    controllers:
      main:
        initContainers:
          init-perms:
            image:
              repository: busybox
              tag: stable
            command:
              - /bin/sh
              - -lc
            args:
              - |
                set -e
                echo "Preparing writable paths for non-root entrypoint..."

                # PVC-backed data dir (needed for /data/jwt mktemp)
                mkdir -p /data
                chown -R 1000:1000 /data
                chmod -R 0775 /data

                # Writable /etc/profile.d (mounted emptyDir)
                mkdir -p /etc/profile.d
                chown -R 1000:1000 /etc/profile.d
                chmod -R 0775 /etc/profile.d

                # Writable /etc/environment (mounted file via subPath)
                touch /etc/environment
                chown 1000:1000 /etc/environment
                chmod 0664 /etc/environment

                # Writable /root/.bashrc (root home mounted emptyDir)
                mkdir -p /root
                touch /root/.bashrc
                chown -R 1000:1000 /root
                chmod 0775 /root
                chmod 0664 /root/.bashrc

                echo "Done. Debug:"
                ls -ld /data /etc/profile.d /root || true
                ls -l /etc/environment /root/.bashrc || true
            securityContext:
              runAsUser: 0
              runAsGroup: 0
              runAsNonRoot: false

        containers:
          main:
            image:
              repository: ghcr.io/dispatcharr/dispatcharr
              tag: 0.14.0
              pullPolicy: IfNotPresent
            env:
              - name: TZ
                value: America/Los_Angeles
              - name: DISPATCHARR_ENV
                value: aio
              - name: DISPATCHARR_LOG_LEVEL
                value: info
            ports:
              - name: http
                containerPort: 9191
                protocol: TCP

    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000

    service:
      main:
        controller: main
        type: ClusterIP
        ports:
          http:
            port: 9191

    persistence:
      data:
        enabled: true
        existingClaim: dispatcharr
        advancedMounts:
          main:
            main:
              - path: /data
            init-perms:
              - path: /data

      # make /etc/profile.d writable without changing the image layer
      profile-d:
        type: emptyDir
        advancedMounts:
          main:
            main:
              - path: /etc/profile.d
            init-perms:
              - path: /etc/profile.d

      # mount a writable /etc/environment *file* (not the whole /etc)
      etc-environment:
        type: emptyDir
        advancedMounts:
          main:
            main:
              - path: /etc/environment
                subPath: environment
            init-perms:
              - path: /etc/environment
                subPath: environment

      # make /root writable for /root/.bashrc writes
      root-home:
        type: emptyDir
        advancedMounts:
          main:
            main:
              - path: /root
            init-perms:
              - path: /root