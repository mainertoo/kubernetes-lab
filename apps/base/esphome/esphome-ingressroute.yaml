apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: esphome-ingressroute
  namespace: esphome
  annotations:
    gethomepage.dev/href: "https://esphome.lab.mainertoo.com"
    gethomepage.dev/enabled: "true"
    gethomepage.dev/name: "ESPHome"
    gethomepage.dev/description: "ESPHome dashboard (Bluetooth proxy, sensors, OTA)"
    gethomepage.dev/group: "Automation"
    gethomepage.dev/icon: "https://cdn.jsdelivr.net/gh/selfhst/icons/svg/esphome.svg"
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`esphome.lab.mainertoo.com`)
      kind: Rule
      services:
        - name: esphome
          port: 80
      middlewares:
        - name: authentik-sso
          namespace: traefik-system
  tls: {}