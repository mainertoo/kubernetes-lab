apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: excalidash-config
  namespace: excalidash
  labels:
    backup: daily
    backup-engine: kopia
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd
  resources:
    requests:
      storage: 1Gi
