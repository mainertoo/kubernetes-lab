apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app riven
  namespace: media
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  maxHistory: 2
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false

  values:
    controllers:
      riven:
        pod:
          # Run where Zurg (/mnt/zurg) is definitely mounted and tested
          nodeSelector:
            kubernetes.io/hostname: mainertoo-k3s-worker-2

        containers:
          postgres:
            image:
              repository: postgres
              tag: 17.0-alpine3.20
            env:
              # EXACTLY like riven-pg-debug
              - name: PGDATA
                value: /var/lib/postgresql/data/pgdata
            envFrom:
              - secretRef:
                  name: riven-postgres-secret
              - configMapRef:
                  name: riven-postgres-configmap
            probes:
              readiness:
                enabled: true
                custom: true
                spec:
                  exec:
                    command: ["pg_isready", "-U", "postgres"]
                  initialDelaySeconds: 10
                  periodSeconds: 5

          backend:
            image:
              repository: docker.io/spoked/riven
              tag: latest
            env:
              - name: TZ
                value: America/Los_Angeles
            envFrom:
              - secretRef:
                  name: riven-backend-secret
              - configMapRef:
                  name: riven-backend-configmap
            dependsOn: postgres

          frontend:
            image:
              repository: docker.io/spoked/riven-frontend
              tag: latest
            env:
              - name: TZ
                value: America/Los_Angeles
            envFrom:
              - secretRef:
                  name: riven-frontend-secret
              - configMapRef:
                  name: riven-frontend-configmap
            dependsOn: backend

    service:
      frontend:
        controller: riven
        type: ClusterIP
        ports:
          http:
            port: &portfrontend 3000

      backend:
        controller: riven
        type: ClusterIP
        ports:
          http:
            port: &portbackend 8080

      postgres:
        controller: riven
        type: ClusterIP
        ports:
          postgres:
            port: &portpostgres 5432
            targetPort: 5432

    persistence:
      # ⚠️ NO more "data" PVC for backend here – let backend use container FS for /riven/data

      # HostPath rclone mount for Zurg – backend only
      rclone:
        type: hostPath
        hostPath: /mnt/zurg/
        advancedMounts:
          riven:
            backend:
              - path: /mnt/zurg
                readOnly: true

      # Media library PVC – backend only (just like your debug volume pod)
      media:
        existingClaim: shared-media-pvc
        advancedMounts:
          riven:
            backend:
              - path: /mnt/library

      # Postgres data PVC (only Postgres uses riven-backend-pvc)
      database:
        existingClaim: riven-backend-pvc
        advancedMounts:
          riven:
            postgres:
              - path: /var/lib/postgresql/data