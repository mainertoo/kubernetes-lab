apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: plex
resources:
  # - plex-namespace.yaml   # NAMESPACE change to media
  - plex-ingressroute.yaml
  - plex-config-pvc.yaml      # NEW – Plex metadata/config on CephFS
  # - plex-media-pvc.yaml       # NEW – QNAP media via NFS- plex-release.yaml
  - plex-release.yaml
  - plex-config-perms-job.yaml