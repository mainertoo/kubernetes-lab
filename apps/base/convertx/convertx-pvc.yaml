apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: convertx
  namespace: convertx
spec:
  storageClassName: ceph-rbd
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 5Gi