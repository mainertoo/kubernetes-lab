apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: lcshare
resources:
  - lcshare-namespace.yaml
  # PVC owned by volsync-v2 (bootstrap on first deploy, plain after settle)
  # - lcshare-pvc.yaml
  - lcshare-release.yaml
  - lcshare-ingressroute.yaml