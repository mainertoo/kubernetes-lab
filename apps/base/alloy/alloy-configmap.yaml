apiVersion: v1
kind: ConfigMap
metadata:
  name: alloy-config
  namespace: alloy
data:
  config.alloy: |-
    logging {
      level  = "info"
      format = "logfmt"
    }

    discovery.kubernetes "pods" {
      role = "pod"
    }

    // Read pod logs from Kubernetes API
    loki.source.kubernetes "pod_logs" {
      targets    = discovery.kubernetes.pods.targets
      forward_to = [loki.process.k8s.receiver]
    }

    // Add useful labels + forward to Loki
    loki.process "k8s" {
      forward_to = [loki.write.default.receiver]

      stage.labels {
        values = {
          namespace = "namespace",
          pod       = "pod",
          container = "container",
          node      = "node",
        }
      }
    }

    // Push to Loki
    loki.write "default" {
      endpoint {
        url = "http://loki.loki.svc.cluster.local:3100/loki/api/v1/push"
      }
    }