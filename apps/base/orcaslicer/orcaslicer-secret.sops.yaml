apiVersion: v1
kind: Secret
metadata:
  name: orcaslicer-secret
  namespace: orcaslicer
type: Opaque
stringData:
  # KasmVNC session credentials. The image defaults to abc/abc if these are
  # unset — set them to something real before this is applied.
  CUSTOM_USER: CHANGEME
  PASSWORD: CHANGEME
