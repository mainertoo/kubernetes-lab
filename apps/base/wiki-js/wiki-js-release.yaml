apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app wiki-js
  namespace: wiki-js
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 3.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      reconcileStrategy: ChartVersion

  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  uninstall:
    keepHistory: false

  values:
    controllers:
      main:
        type: deployment
        replicas: 1
        containers:
          # 1) Wiki.js application container
          app:
            image:
              repository: ghcr.io/requarks/wiki
              tag: "2.5.308"
              pullPolicy: IfNotPresent

            # Use env from the SOPS Secret
            envFrom:
              - secretRef:
                  name: wiki-js-secret

            # Non-secret env overrides (optional)
            env:
              TZ: America/Los_Angeles
              PORT: "3000"

            resources:
              requests:
                cpu: 100m
                memory: 256Mi
              limits:
                cpu: 1000m
                memory: 1Gi

          # 2) Postgres DB sidecar (same pattern as your docker-compose)
          db:
            image:
              repository: postgres
              tag: "15-alpine"

            envFrom:
              - secretRef:
                  name: wiki-js-secret

            env:
              # Postgres official env variable names
              POSTGRES_DB:
                valueFrom:
                  secretKeyRef:
                    name: wiki-js-secret
                    key: POSTGRES_DB
              POSTGRES_USER:
                valueFrom:
                  secretKeyRef:
                    name: wiki-js-secret
                    key: POSTGRES_USER
              POSTGRES_PASSWORD:
                valueFrom:
                  secretKeyRef:
                    name: wiki-js-secret
                    key: POSTGRES_PASSWORD

            resources:
              requests:
                cpu: 50m
                memory: 256Mi
              limits:
                cpu: 500m
                memory: 1Gi

            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  exec:
                    command:
                      - /bin/sh
                      - -c
                      - |
                        pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB" -h 127.0.0.1
                  initialDelaySeconds: 30
                  periodSeconds: 10
                  timeoutSeconds: 5
                  failureThreshold: 5
              readiness:
                enabled: true
                custom: true
                spec:
                  exec:
                    command:
                      - /bin/sh
                      - -c
                      - |
                        pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB" -h 127.0.0.1
                  initialDelaySeconds: 10
                  periodSeconds: 10
                  timeoutSeconds: 5
                  failureThreshold: 5

    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        # Let the image's own user work; don't override with 1000 so we don't
        # break /wiki contents in the container image.
        # If you really want 1000 later, we can add it back once everything is stable.
        fsGroupChangePolicy: "OnRootMismatch"

    service:
      main:
        enabled: true
        controller: main
        ports:
          http:
            port: 3000

      db:
        enabled: true
        controller: main
        ports:
          postgres:
            enabled: true
            port: 5432

    persistence:
      # 🚨 Only persist Postgres data; do NOT mount anything at /wiki
      db-data:
        enabled: true
        existingClaim: wiki-js-postgres
        globalMounts:
          - path: /var/lib/postgresql/data
            subPath: pgdata