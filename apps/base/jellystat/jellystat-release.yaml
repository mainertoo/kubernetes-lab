apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app jellystat
  namespace: jellystat
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  maxHistory: 2

  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    force: true
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false

  values:
    controllers:
      # =========================
      # Postgres for Jellystat
      # =========================
      postgres:
        replicas: 1

        containers:
          app:
            image:
              repository: postgres
              tag: "15.17"
              pullPolicy: IfNotPresent

            # These come from your SOPS secret:
            #   POSTGRES_USER
            #   POSTGRES_PASSWORD
            envFrom:
              - secretRef:
                  name: jellystat-secret

            env:
              # Name of the DB Jellystat will use
              - name: POSTGRES_DB
                value: jellystat

              # Use a subdirectory so we don't hit the lost+found issue
              - name: PGDATA
                value: /var/lib/postgresql/data/pgdata

      # =========================
      # Jellystat app
      # =========================
      jellystat:
        replicas: 1

        containers:
          app:
            image:
              repository: cyfershepard/jellystat
              tag: 1.1.10
              pullPolicy: IfNotPresent

            # Pull POSTGRES_USER / POSTGRES_PASSWORD (and others) from the secret
            envFrom:
              - secretRef:
                  name: jellystat-secret

            env:
              - name: TZ
                value: America/Los_Angeles

              # Internal service name for Postgres
              # (Service will be named "jellystat-postgres")
              - name: POSTGRES_IP
                value: jellystat-postgres

              - name: POSTGRES_PORT
                value: "5432"

              # DB name the app will connect to
              - name: POSTGRES_DB
                value: jellystat

              # Wire your SOPS secret key into the env var the app expects
              - name: JWT_SECRET
                valueFrom:
                  secretKeyRef:
                    name: jellystat-secret
                    key: JWT_SECRET

              # You explicitly wanted this
              - name: REJECT_SELF_SIGNED_CERTIFICATES
                value: "false"

    # -------------------------
    # Services
    # -------------------------
    service:
      # Jellystat HTTP service (what Traefik / IngressRoute will target)
      app:
        controller: jellystat
        type: ClusterIP
        ports:
          http:
            port: 3000

      # Internal Postgres service – used only by Jellystat
      postgres:
        controller: postgres
        type: ClusterIP
        ports:
          postgres:
            port: 5432

    # -------------------------
    # Persistence (PVCs)
    # -------------------------
    persistence:
      # RBD-backed Postgres data
      postgres-data:
        existingClaim: jellystat-postgres-data
        advancedMounts:
          postgres:
            app:
              - path: /var/lib/postgresql/data

      # Backup data (RBD or CephFS depending on your PVC)
      backup-data:
        existingClaim: jellystat-backup-data
        advancedMounts:
          jellystat:
            app:
              - path: /app/backend/backup-data