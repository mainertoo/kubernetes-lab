apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wallos
  labels:
    # Phase 5b of the volsync label-driven restore project.
    # ClusterPolicy volsync-pvc-backup-restore matches this label and
    # generates volsync-wallos Secret + wallos-backup RS + RD in this ns.
    backup: daily
    backup-engine: restic
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd
  resources:
    requests:
      storage: 5Gi
