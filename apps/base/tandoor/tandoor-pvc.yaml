apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tandoor-data
  namespace: tandoor
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd
  resources:
    requests:
      storage: 10Gi