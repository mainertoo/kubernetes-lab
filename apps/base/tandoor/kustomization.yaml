apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tandoor
resources:
  - tandoor-namespace.yaml
  # PVC owned by volsync-v2 (bootstrap on first deploy, plain after settle).
  # Renamed from tandoor-data → tandoor during Phase 1c migration.
  # - tandoor-pvc.yaml
  - tandoor-sso-secret.sops.yaml
  - tandoor-release.yaml
  - tandoor-ingressroute.yaml