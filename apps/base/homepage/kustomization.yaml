apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: homepage
resources:
  - homepage-namespace.yaml
  - homepage-default-configmap.yaml
  # Phase 5b: PVC ownership transferred from components/volsync-v2 to a
  # static manifest carrying `backup: daily`. ClusterPolicy
  # `volsync-pvc-backup-restore` generates the volsync trio off the label.
  - homepage-pvc.yaml
  - homepage-rbac.yaml
  - homepage-release.yaml
  - homepage-ingressroute.yaml