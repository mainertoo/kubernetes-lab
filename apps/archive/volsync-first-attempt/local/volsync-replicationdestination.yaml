apiVersion: volsync.backube/v1alpha1
kind: ReplicationDestination
metadata:
  name: "${APP}-bootstrap"
  labels:
    kustomize.toolkit.fluxcd.io/ssa: IfNotPresent
spec:
  trigger:
    manual: restore-once
  restic:
    repository: "${APP}-volsync-local"
    copyMethod: Snapshot
    volumeSnapshotClassName: "${VOLSYNC_SNAPSHOTCLASS:=ceph-rbd-snapclass}"

    cacheStorageClassName: "${VOLSYNC_CACHE_STORAGECLASS:=ceph-rbd}"
    cacheAccessModes: ["${VOLSYNC_CACHE_ACCESSMODES:=ReadWriteOnce}"]
    cacheCapacity: "${VOLSYNC_CACHE_CAPACITY:=1Gi}"

    # IMPORTANT:
    # Destination PVC must match the APP storage for RWX apps like CephFS.
    storageClassName: "${APP_STORAGECLASS:=ceph-rbd}"
    accessModes: ["${VOLSYNC_ACCESSMODES:=${VOLSYNC_ACCESSMODE:=ReadWriteOnce}}"]
    capacity: "${CAPACITY:=1Gi}"

    moverSecurityContext:
      runAsUser: ${VOLSYNC_PUID:=1000}
      runAsGroup: ${VOLSYNC_PGID:=1000}
      fsGroup: ${VOLSYNC_PGID:=1000}

    enableFileDeletion: true
    cleanupCachePVC: true
    cleanupTempPVC: true