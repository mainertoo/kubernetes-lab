apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system
# rke2-multus (K3s-official Multus chart) — STAGING ONLY, Phase 1 of the
# Multus + HA failover runbook (docs/multus-ha-failover-runbook.md).
# Merging this DEPLOYS Multus to the staging cluster. Do not merge until the
# runbook's Phase-1 prereqs are met (flannel conflist backed up, rollback ready).
resources:
  - repository.yaml
  - release.yaml
