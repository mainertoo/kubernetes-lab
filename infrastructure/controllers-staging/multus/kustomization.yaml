apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system
# rke2-multus (K3s-official Multus chart) — STAGING ONLY, Phase 1 of the
# Multus + HA failover runbook (docs/multus-ha-failover-runbook.md).
# Reconciled by the dedicated `infra-multus` Flux Kustomization (commented out in
# clusters/staging/infrastructure.yaml) — nothing here deploys until that block is
# uncommented. Activate only after the flannel conflist is backed up + rollback ready.
resources:
  - repository.yaml
  - release.yaml
