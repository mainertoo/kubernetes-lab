apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - snapshot-controller-namespace.yaml
  - snapshot-controller-repository.yaml
  - snapshot-controller-release.yaml
  - snapshot-controller-pdb.yaml

  # Snapshot classes (required for VolumeSnapshots + VolSync Snapshot copyMethod)
  - volumesnapshotclass-ceph-rbd.yaml
  - volumesnapshotclass-cephfs.yaml