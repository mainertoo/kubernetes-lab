apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - volsync-repository.yaml
  - volsync-release.yaml
  - volsync-namespace.yaml
  - volsync-servicemonitor.yaml

# NOTE:
# MutatingAdmissionPolicy (v1alpha1) + NFS injection is environment-specific.
# Once you decide your VolSync repository strategy, we can re-enable:
#  - ./mutations