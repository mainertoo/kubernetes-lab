apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - kyverno-namespace.yaml
  - kyverno-release.yaml
  # policies/ is applied in a follow-up commit after Kyverno CRDs are installed
