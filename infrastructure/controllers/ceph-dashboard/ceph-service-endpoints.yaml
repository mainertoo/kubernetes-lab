apiVersion: v1
kind: Service
metadata:
  name: ceph-dashboard
  namespace: ceph-dashboard
spec:
  type: ClusterIP
  ports:
    - name: https
      port: 8443
      targetPort: 8443
---
apiVersion: v1
kind: Endpoints
metadata:
  name: ceph-dashboard
  namespace: ceph-dashboard
subsets:
  - addresses:
      # The Ceph dashboard runs ONLY on the ACTIVE mgr. Standby mgrs return
      # HTTP 303 redirecting to the active mgr's internal URL
      # (https://<active>:8443/), which a browser cannot reach through the
      # ingress. So this MUST point at exactly the active mgr — do NOT list all
      # three like ceph-mgr-metrics does. Re-pin on mgr failover.
      #
      # Confirm the active mgr before changing this:
      #   ceph mgr services | grep dashboard     -> "https://<active-ip>:8443/"
      #
      #   192.168.99.12 = pve-whistler  (active as of 2026-08-15)  <-- current
      #   192.168.99.13 = pve-zermatt   (standby)
      #   192.168.99.14 = pve-mammoth   (standby)
      - ip: 192.168.99.12
    ports:
      - name: https
        port: 8443
        protocol: TCP