apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # If you already have NADs working (you do), the CRD is already present.
  # Do NOT apply a CRD from a file that also contains RBAC/DaemonSet.
  #
  # Keep Multus DS + plugin installer only:
  - multus-daemonset.yaml
  - cni-plugins-installer.yaml