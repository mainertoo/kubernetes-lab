apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # CRD must exist before any NetworkAttachmentDefinitions (NADs) can be applied
  - nad-crd.yaml

  # Multus DaemonSet (cluster-wide), tailored for k3s paths
  - multus-daemonset.yaml