apiVersion: v1
kind: ServiceAccount
metadata:
  name: multus
  namespace: kube-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: multus
rules:
  - apiGroups: [""]
    resources: ["pods", "pods/status", "nodes", "namespaces"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["k8s.cni.cncf.io"]
    resources: ["network-attachment-definitions"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: multus
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: multus
subjects:
  - kind: ServiceAccount
    name: multus
    namespace: kube-system
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: kube-multus-ds
  namespace: kube-system
  labels:
    app: multus
spec:
  selector:
    matchLabels:
      app: multus
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: multus
    spec:
      hostNetwork: true
      serviceAccountName: multus
      priorityClassName: system-node-critical
      tolerations:
        - operator: Exists

      initContainers:
        - name: install-multus-binary
          image: ghcr.io/k8snetworkplumbingwg/multus-cni:v4.0.2
          imagePullPolicy: IfNotPresent
          securityContext:
            privileged: true
          command: ["/bin/sh","-lc"]
          args:
            - |
              set -eux

              SRC="/usr/src/multus-cni/bin/multus"
              test -f "${SRC}"

              K3S_DATA="/host/var/lib/rancher/k3s/data"

              echo "Host k3s data dir listing:"
              ls -la "${K3S_DATA}" || true
              ls -la "${K3S_DATA}/current" || true

              # Resolve the *real* k3s current dir target
              CUR="$(readlink -f "${K3S_DATA}/current" 2>/dev/null || true)"
              if [ -n "${CUR}" ] && [ -d "${CUR}/bin" ]; then
                BIN="${CUR}/bin"
              else
                # Fallback: pick newest */bin under k3s data
                BIN="$(ls -d1 ${K3S_DATA}/*/bin 2>/dev/null | sort | tail -n1 || true)"
              fi

              test -n "${BIN}"
              mkdir -p "${BIN}"
              install -m 0755 "${SRC}" "${BIN}/multus"

              # Optional compatibility/debug bin dir
              OPT_DIR="/host/opt/cni/bin"
              mkdir -p "${OPT_DIR}"
              install -m 0755 "${SRC}" "${OPT_DIR}/multus"

              echo "Installed multus to:"
              ls -la "${BIN}/multus" || true
              ls -la "${OPT_DIR}/multus" || true

              # Also show what /var/lib/rancher/k3s/data/current/bin points to on host
              echo "Host current/bin view:"
              ls -la "/host/var/lib/rancher/k3s/data/current/bin" || true
              ls -la "/host/var/lib/rancher/k3s/data/current/bin/multus" || true
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
          volumeMounts:
            - name: k3s-data
              mountPath: /host/var/lib/rancher/k3s/data
            - name: cnibin
              mountPath: /host/opt/cni/bin

      containers:
        - name: kube-multus
          image: ghcr.io/k8snetworkplumbingwg/multus-cni:v4.0.2
          imagePullPolicy: IfNotPresent
          securityContext:
            privileged: true
          command: ["/thin_entrypoint"]
          args:
            - --multus-conf-file=auto
            - --multus-autoconfig-dir=/host/var/lib/rancher/k3s/agent/etc/cni/net.d
            - --cni-conf-dir=/host/var/lib/rancher/k3s/agent/etc/cni/net.d

            # Tell multus where to look for plugins on k3s.
            # This path is written into generated config (binDir).
            - --cni-bin-dir=/host/var/lib/rancher/k3s/data/current/bin
            - --additional-bin-dir=/var/lib/rancher/k3s/data/current/bin

            - --multus-master-cni-file-name=10-flannel.conflist
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              memory: 256Mi
          volumeMounts:
            - name: k3s-cniconf
              mountPath: /host/var/lib/rancher/k3s/agent/etc/cni/net.d
            - name: k3s-data
              mountPath: /host/var/lib/rancher/k3s/data
            - name: cnibin
              mountPath: /host/opt/cni/bin

      volumes:
        - name: k3s-cniconf
          hostPath:
            path: /var/lib/rancher/k3s/agent/etc/cni/net.d
            type: DirectoryOrCreate
        - name: k3s-data
          hostPath:
            path: /var/lib/rancher/k3s/data
            type: DirectoryOrCreate
        - name: cnibin
          hostPath:
            path: /opt/cni/bin
            type: DirectoryOrCreate