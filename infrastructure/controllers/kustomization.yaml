apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Ceph CSI drivers
  - ceph-csi-cephfs
  - ceph-csi-rbd

  # cert-manager + Traefik
  - cert-manager
  - traefik-proxy

  # CSI drivers & storageclasses
  - nfs-csi
  - storage          # includes cephfs / ceph-rbd / nfs-qnap storageclasses
  - storage-nfs      # NFS storage namespace/RBAC/SC
