apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: "${APP}"
spec:
  sourcePVC: "${APP}"
  trigger:
    # twice daily default (keep your original cadence)
    schedule: "05 00/12 * * *"
  restic:
    repository: "${APP}-volsync"
    pruneIntervalDays: 14

    copyMethod: Snapshot
    volumeSnapshotClassName: "${VOLSYNC_SNAPSHOTCLASS:=cephfs-snapclass}"

    # PiT volume settings (defaults are source PVC values; keep explicit for clarity)
    storageClassName: "${VOLSYNC_STORAGECLASS:=cephfs}"
    accessModes: ["${VOLSYNC_SNAP_ACCESSMODES:=${VOLSYNC_ACCESSMODES:=ReadWriteMany}}"]
    capacity: "${CAPACITY:=10Gi}"

    # cache can be RBD even if the app PVC is CephFS
    cacheStorageClassName: "${VOLSYNC_CACHE_STORAGECLASS:=ceph-rbd}"
    cacheAccessModes: ["${VOLSYNC_CACHE_ACCESSMODES:=ReadWriteOnce}"]
    cacheCapacity: "${VOLSYNC_CACHE_CAPACITY:=1Gi}"

    moverSecurityContext:
      runAsUser: ${VOLSYNC_PUID:=1000}
      runAsGroup: ${VOLSYNC_PGID:=1000}
      fsGroup: ${VOLSYNC_PGID:=1000}

    retain:
      hourly: 24
      daily: 7
      weekly: 5
      monthly: 3